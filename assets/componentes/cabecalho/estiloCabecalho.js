import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    header: {
        width: '100%',
        alignItems: 'center',
        justifyContent: 'center',
        height: 60,
        backgroundColor: '#a53a3aff',
    },
    texto: {
        color: '#FFFFFF',
        fontSize: 20,
    }
})

export default styles;