import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    header: {
        width: '100%',
        alignItems: 'center',
        justifyContent: 'center',
        height: 60,
        backgroundColor: '#da2b2bff',
    },
    texto: {
        color: '#FFFFFF',
        fontSize: 20,
    }
})

export default styles;